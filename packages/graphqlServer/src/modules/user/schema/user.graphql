type User {
  id: ID!
  email: String
  profile: Profile
  role: String
  refreshToken: String
}

type Profile {
  id: ID
  userId: ID
  firstName: String
  lastName: String
}

type Auth {
  user: User!
  accessToken: String!
  refreshToken: String!
}
